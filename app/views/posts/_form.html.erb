<%= render  "errors" %>
<%= form_with(model: @post, local: true) do |f| %>
  <div class="field">
    <%= f.label :title %> <br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %> <br>
    <%= f.text_area :body %>
  </div>

  <%= f.submit %>
<% end %>
